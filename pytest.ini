[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py

markers =
    unit: Fast logic tests (run locally)
    integration: System-wide tests (run against Minikube)
    contract: Schema validation tests (run against Minikube)

addopts = -v --tb=short -W ignore::pytest.PytestRemovedIn9Warning