apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: triage-agent-monitor
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      service: triage-agent
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: metrics
      interval: 15s
